package ${package}.mapper;

import java.util.List;
import com.ptshop.util.MyMapper;
import com.ptshop.model.banner.${className};
/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends MyMapper<${className}> {

    /**
    * 根据条件查询，主键倒序排序
    */
    List<${className}> selectByQuery(${className} ${classname});

}
